#include <stdio.h>

int main()
{
	int T; scanf("%d", &T);
	
	for (int i = 0; i < T; i++)
	{
		int N;
		scanf("%d", &N);
		int number[N];
		for (int j = 0; j < N; j++)
		{
			scanf("%d", &number[i]);
		}
		int P, Q;
		scanf("%d %d", &P, &Q);
		
		printf("Case #%d :")
	}
	
	return 0;
}
